<div class="header">
  <div class="container-header-dashboard container">
    <h1 class="fw-bold">Hello <%= current_user.first_name %> 🎉</h1>
    <p class="welcome">Welcome back home !</p>

    <div class="banner-overview row">
      <div class="col-3 d-flex justify-content-between align-items-center">
        <div>
          <h6>Propriétés</h6>
          <span>1</span>
        </div>
        <span><%= render :partial => 'svg/property' %></span>
      </div>
      <div class="col-3 d-flex justify-content-between align-items-center">
        <div>
          <h6>Chantiers en cours</h6>
          <span>1</span>
        </div>
        <span><%= render :partial => 'svg/chantier' %></span>
      </div>
      <div class="col-3 d-flex justify-content-between align-items-center">
        <div>
          <h6>Messages non vus</h6>
          <span>1</span>
        </div>
        <span><%= render :partial => 'svg/messages' %></span>
      </div>
      <div class="col-3 d-flex justify-content-between align-items-center">
        <div>
          <h6>Paiements en attente</h6>
          <span>1</span>
        </div>
        <span><%= render :partial => 'svg/paiement' %></span>
      </div>
    </div>
  </div>
</div>

<div class="container mt-5">
<h3>Mes projets en cours</h3>
<% if @projects.empty? %>
    <div>
      <div>
        <section class="section">
          <div class="d-flex justify-content-start align-items-center">
            <div>
              <h3>Pas encore de projet ?</h3>
              <p>Créer votre projet en quelques clics</p>
              <%= link_to "Créer mon projet", new_project_path, class: "btn btn-outline-dark" %>
            </div>
          </div>
        </section>
      </div>
    </div>
<% else %>
  <% @projects.each do |project| %>
    <div class="card-project mt-5">
      <%= link_to project_path(project), class: 'link-card' do %>
        <% if project.photo.attached? %>
          <img class="card-img-top img-size" src="<%= cl_image_path project.photo.key %>" alt="Card image cap">
        <% else %>
          <img class="card-img-top img-size" src="https://images.unsplash.com/photo-1600585154340-be6161a56a0c?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=2070&q=80" alt="Villa">
        <% end  %>
        <div class="text-decoration-none">
          <p class="card-text"><strong><%= project.name %></strong></p>
          <p><%= project.description %></p>
        </div>
        <p class="card-text"><em><%= project.urgency %></em></p>
        <div class="d-flex justify-content-between">
          <div class="d-flex"><span class="badge rounded-5 text-bg-dark mb-3"><%= project.budget %></span></div>
        </div>
      <% end %>
    </div>
  <% end %>
<% end %>
</div>
