<h1>Voici les pièces de ce projet :</h1>
<% @rooms.each do |room| %>
  <%= room.room_type %>
  <%#= link_to "Détruire cette pièce", destroy_room_path(room), class: "btn btn-danger" %>
  <% room.photos.each do |photo| %>
  <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
  <% end %>
<% end %>
